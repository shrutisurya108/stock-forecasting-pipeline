# ══════════════════════════════════════════════════════════════════════════════
# Python 3.14 compatible — verified against pyreadiness.org/3.14 (Apr 2026)
# ══════════════════════════════════════════════════════════════════════════════

# ── Core Data ─────────────────────────────────────────────────────────────────
# numpy, pandas, scikit-learn all ship cp314 wheels (pyreadiness ✓)
numpy>=2.1.0
pandas>=2.2.3
scikit-learn>=1.5.2
scipy>=1.14.0                # cp314 wheel available (pyreadiness ✓)

# ── Stock Data ────────────────────────────────────────────────────────────────
yfinance>=0.2.54              # pure Python; works on 3.14

# ── Time-Series Models ────────────────────────────────────────────────────────

# ARIMA — statsmodels is pure Python; pmdarima 2.1.1 ships cp314 wheels
statsmodels>=0.14.4
pmdarima==2.1.1               # cp314 wheel available (added 3.14 support Nov 2025)

# Prophet — pure-Python package; 1.3.0 is the latest release (Jan 2026)
prophet==1.3.0

# PyTorch — 2.11.0 is the first official cp314 release (Mar 2026)
# CPU-only; swap index-url below for GPU builds if needed
torch==2.11.0

# ── AWS / Storage ─────────────────────────────────────────────────────────────
boto3>=1.35.0                 # pure Python (pyreadiness ✓)

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.39.0             # pure Python; installs fine on 3.14
plotly>=5.24.0                # pure Python; installs fine on 3.14

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1          # pure Python (pyreadiness ✓)
pyarrow>=17.0.0               # cp314 wheel available (pyreadiness ✓)
requests>=2.32.0              # pure Python (pyreadiness ✓)
